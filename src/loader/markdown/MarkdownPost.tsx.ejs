
import * as React from 'react';
import { PureComponent, Content } from 'paramorph';

<% includes.forEach(file => { %>
import <%= file.name %> from '@website/<%= file.path.substring(2) %>';
<% }) %>

/**
 * @author Maciej Chałapuk (maciej@chalapuk.pl)
 */
export class MarkdownPost extends PureComponent<{}, {}> {
  render() {
    const { paramorph, post, history } = this.context;

    return (
      <Content limit={ 5 } { ...this.props }>
        <%- html %>
      </Content>
    );
  }
}

export default MarkdownPost;

